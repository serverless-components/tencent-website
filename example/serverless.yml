name: websitedemo
org: test
app: websiteApp
component: website@dev
stage: dev

inputs:
  src:
    src: ./src
    index: index.html
    error: index.html
  region: ap-guangzhou
  bucketName: my-bucket
  protocol: https
  hosts:
    - host: fullstack.yugasun.com
      autoRefesh: true
      async: false
      area: overseas
      forceRedirect:
        switch: on
        redirectType: https
        redirectStatusCode: 301
      https:
        switch: on
        http2: on
        certInfo:
          certId: 'Z67aCPcj'
